{
    "require": {
        "netflex/framework": "^4.0"
    },
    "require-dev": {
        "code-lts/doctum": "^5.3"
    },
    "scripts": {
        "post-install-cmd": [
            "set -e && php doctum.phar update doctum.config.php || true"
        ],
        "post-update-cmd": [
            "set -e && php doctum.phar update doctum.config.php || true"
        ]
    }
}